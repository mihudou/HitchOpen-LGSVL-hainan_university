builtin_interfaces/Time stamp

RppTrajectory trajectory

uint8 current_ttl_index

float64 target_speed

# requested acceleration rate to hit the target speed
# if 0 then ignore
float64 target_speed_rate

bool path_exists

StopType stop_type